import { Router, Request, Response } from 'express';
import { CandidateControllerBackEnd, InitServerIdentity } from './convector';

const router: Router = Router();

export const TrainingOfferExpressController: Router = router;